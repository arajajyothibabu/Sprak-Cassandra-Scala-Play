# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /hello                      controllers.Application.hello
GET     /insert                     controllers.Application.insert
GET     /group                      controllers.Application.group
GET     /join                       controllers.Application.join
GET     /groupby                    controllers.Application.groupBy
GET     /partion-grouping           controllers.Application.partionGrouping
GET     /broadcast-join             controllers.Application.broadcastJoin

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    